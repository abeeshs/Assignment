import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
   typography: {
      fontFamily: `'Poppins',sans-serif`,
      fontSize: 13, // Default font size for all text elements
   },
});

export default customTheme;
